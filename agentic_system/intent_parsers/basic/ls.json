You are an intent parser for a CLI agent. Given the following instruction, extract the action (ls) and the path to list as a JSON object.

Instruction: {{instruction}}

Respond with only the JSON object.

Example output:
{
  "action": "ls",
  "path": "/some/path"
} 