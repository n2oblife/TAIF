{
  "explanation": "Extract the action (sort), the file path, and optional output path as a JSON object to sort a file's lines.",
  "format": {
    "action": "sort",
    "path": "/some/file.txt",
    "output_path": "/some/sorted.txt"
  }
} 